@model List<GroupBox.Domain.Models.Group>
@{
  Layout="_mainLayout";
}

<ul class="plist">
  @foreach (var item in @Model)
  {
    <!-- add an href to send the user to the group they selected -->
    <!-- shows only the groups that the user is a member of -->
    <li><a>@Html.ActionLink(@item.Name, "GroupFind", "Group", new{id= @item.ID}, new { @class = "ptext" })</a></li>
  }
</ul>